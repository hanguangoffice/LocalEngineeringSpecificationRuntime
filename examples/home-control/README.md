# Home Control Example

Copy `built_in_profiles/aspice-lite` to `profiles/aspice-lite`, then run:

```powershell
lesr index examples/home-control
lesr artifact-get examples/home-control REQ-SW-0001
```
