# DD-014 关系默认绑定策略

- 状态：ACCEPTED
- 决策：
  - 普通长期工程关系默认 Logical Binding；
  - Approval、Evidence、Baseline、Execution Result 默认 Pinned Revision Binding；
  - Fragment Binding 只用于局部定位，默认不满足正式追踪；
  - Profile Relation Type 可收紧绑定要求。
